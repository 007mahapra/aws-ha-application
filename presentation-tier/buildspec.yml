version: 0.2

phases:
  pre_build:
    commands:
      - echo "Logging into Amazon ECR..."
      - aws ecr get-login-password --region <your-region> | docker login --username AWS --password-stdin <your-account-id>.dkr.ecr.<your-region>.amazonaws.com
      - IMAGE_URI=<your-account-id>.dkr.ecr.<your-region>.amazonaws.com/presentation-tier
  build:
    commands:
      - echo "Building Docker image for presentation-tier..."
      - docker build -t presentation-tier .
      - docker tag presentation-tier:latest $IMAGE_URI:latest
  post_build:
    commands:
      - echo "Pushing Docker image to ECR..."
      - docker push $IMAGE_URI:latest
artifacts:
  files:
    - '**/*'
